export interface DialogLine {
    name: string         // Name of the speaker
    message: string      // Dialog text
    image?: string       // Optional portrait
    side?: "left" | "right" // Side to display portrait
  }
  